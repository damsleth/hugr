"""mnem.mcp - MCP (Model Context Protocol) server for the mnem suite.

Exposes mnem.api functions as MCP tools over stdio (plan 04.1).
HTTP transport is plan 04.6.

Requires the [mcp] extra:

    pipx install "mnem-suite[mcp]"
"""
