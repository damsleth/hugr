"""hugr.mcp - MCP (Model Context Protocol) server for the hugr suite.

Exposes hugr.api functions as MCP tools over stdio (plan 04.1).
HTTP transport is plan 04.6.

Requires the [mcp] extra:

    pipx install "hugr-cli[mcp]"
"""
